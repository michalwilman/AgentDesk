# ========================================
# AgentDesk Production Environment Variables
# ========================================
#
# IMPORTANT: Copy this file to .env.production and fill in your actual values
# NEVER commit .env.production to Git!
#
# Usage:
#   cp env.production.example .env.production
#   nano .env.production  # Edit with your production values
#   docker-compose -f docker-compose.production.yml --env-file .env.production up -d
#

# ========================================
# SUPABASE CONFIGURATION (Production)
# ========================================
# Get these from your PRODUCTION Supabase project
# https://app.supabase.com → Your Project → Settings → API

SUPABASE_URL=https://your-production-project.supabase.co
SUPABASE_ANON_KEY=eyJxxx...your-production-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=eyJxxx...your-production-service-role-key-here

# Maximum file size for document uploads (in bytes)
# Free tier: 52428800 (50MB) | Pro tier: 104857600 (100MB)
SUPABASE_MAX_FILE_SIZE=52428800

# ========================================
# OPENAI CONFIGURATION (Production)
# ========================================
# Get your API key from https://platform.openai.com/api-keys
# IMPORTANT: Use a different key for production than development!

OPENAI_API_KEY=sk-your-production-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ========================================
# APPLICATION URLS (Production)
# ========================================
# Replace with your actual production domains

# Backend API URL (used by frontend and widget)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api

# Frontend Dashboard URL
NEXT_PUBLIC_APP_URL=https://app.yourdomain.com
NEXT_PUBLIC_APP_NAME=AgentDesk

# Widget URL (where the widget is hosted)
NEXT_PUBLIC_WIDGET_URL=https://widget.yourdomain.com

# ========================================
# CORS CONFIGURATION (Production)
# ========================================
# Allow requests from your frontend and widget domains
# Comma-separated list of allowed origins

CORS_ORIGIN=https://app.yourdomain.com,https://widget.yourdomain.com

# ========================================
# SECURITY (Production)
# ========================================
# CRITICAL: Generate a NEW 32-byte (64 hex characters) key for production!
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NEVER reuse your development key in production!

ENCRYPTION_KEY=REPLACE_WITH_NEW_64_CHAR_HEX_KEY_FOR_PRODUCTION

# ========================================
# SERVICE PORTS (Production)
# ========================================
# Ports for Docker containers
# If deploying to cloud, these are usually handled automatically

BACKEND_PORT=3001
FRONTEND_PORT=3000
WIDGET_PORT=3002

# ========================================
# AI CONFIGURATION (Production)
# ========================================
# Chunking configuration for RAG
CHUNK_SIZE=500
CHUNK_OVERLAP=50

# ========================================
# WEB SCRAPER CONFIGURATION (Production)
# ========================================
SCRAPER_TIMEOUT=30000
SCRAPER_MAX_DEPTH=3
SCRAPER_USER_AGENT=AgentDesk-Bot/1.0

# ========================================
# RATE LIMITING (Production)
# ========================================
# Protect your API from abuse
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# ========================================
# OPTIONAL: PAYPAL CONFIGURATION
# ========================================
# For production, use LIVE credentials (not sandbox)
# NEXT_PUBLIC_PAYPAL_CLIENT_ID=your_live_paypal_client_id
# PAYPAL_SECRET=your_live_paypal_secret

# ========================================
# PRODUCTION CHECKLIST
# ========================================
# Before going live, make sure:
# ✅ All XXX placeholders are replaced with real values
# ✅ Using PRODUCTION Supabase project (not dev)
# ✅ Using PRODUCTION OpenAI API key (not dev)
# ✅ ENCRYPTION_KEY is newly generated (not copied from dev)
# ✅ All URLs point to production domains (HTTPS)
# ✅ CORS_ORIGIN includes all your production domains
# ✅ SSL/TLS certificates are configured
# ✅ Database backups are enabled in Supabase
# ✅ Rate limiting is appropriate for your expected traffic
# ✅ This file is added to .gitignore

